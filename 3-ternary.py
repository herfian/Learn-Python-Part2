# Ternary Operator

# Condition_is_True if Condition else condition_is_false

is_friend = False # Condition
can_message = "Message Allowed" if is_friend else "Not Allowed to Message"

print(can_message)

# Output : Not Allowed to Message